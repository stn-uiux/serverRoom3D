export const U_HEIGHT = 0.0445;
export const GRID_SPACING = 1.5;
export const DEVICE_DEPTH = 0.94; // Extended depth to fill the rack interior
export const RACK_WIDTH_STANDARD = 0.6;
export const RACK_WIDTH_WIDE = 1.0;
export const RACK_DEPTH = 1.0;
export const ERROR_MARKER_HEIGHT = 3.0; // Standardized height from floor (or rack base)
